import { Layout } from "@/components/Layout";
import { Breakdown } from "@/components/Breakdown";

const BreakdownPage = () => {
  return (
    <Layout currentPage="breakdown">
      <Breakdown />
    </Layout>
  );
};

export default BreakdownPage;

